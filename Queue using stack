class MyQueue:

    def __init__(self):
        self.push_stack = []
        self.pop_stack = []
        

    def push(self, x: int) -> None:
        self.push_stack.append(x)
        

    def pop(self) -> int:
        if self.empty():return
        if len(self.pop_stack):
            return self.pop_stack.pop()
        else:
            if len(self.push_stack) > 0:
                self.pop_stack.append(self.push_stack[0])
                self.push_stack.remove(self.push_stack[0])
        return self.pop_stack.pop()

    def peek(self) -> int:
        if self.empty():return
        if len(self.pop_stack):
            return self.pop_stack[-1]
        else:
            if len(self.push_stack) > 0:
                self.pop_stack.append(self.push_stack[0])
                self.push_stack.remove(self.push_stack[0])
        return self.pop_stack[-1]
        

    def empty(self) -> bool:
        return len(self.push_stack)==0 and len(self.pop_stack)==0
